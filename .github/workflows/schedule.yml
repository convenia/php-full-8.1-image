name: scheduled-build

on:
  push:
    branches:
      - 'main'
  schedule:
    - cron:  '0 0 * * 6'

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Docker Hub Build
      - run: curl ${{ secrets.BUILD_URL }}